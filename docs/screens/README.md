# Screens

Create one markdown file per screen in this directory before implementation work begins.

Each screen spec should follow the structure in `docs/specs/spec-template.md` and stay focused on user flow, UI rules, state, and edge cases.

## Planned Screens
- `home`
- `capture`
- `sort`
- `insight`
- `drift`
